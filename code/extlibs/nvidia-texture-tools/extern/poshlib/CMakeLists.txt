
SET(POSHLIB_SRCS
	posh.c
	posh.h)

ADD_LIBRARY(posh STATIC ${POSHLIB_SRCS})
TARGET_INCLUDE_DIRECTORIES(posh PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})

SET_TARGET_PROPERTIES(posh PROPERTIES FOLDER "extlibs/nvtex")